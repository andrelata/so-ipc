#include <stdio.h>
#include <sys/types.h> 
#include <sys/stat.h>
#include "client.h"

void
fatal(char *s)
{
	perror(s);
	exit(1);
}


bool connect(char* nickname){

	return TRUE;
}

void disconnect(){

}

char * getSessions(){

	char * p = NULL;
	return p;
}

void joinSession(int sessionID){

}

void createSession(char* name){

}

char * readText(){

	char * p = NULL;
	return p;
}
