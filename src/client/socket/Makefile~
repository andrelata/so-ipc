TARGET = client
OBJS = ./obj/client_socket.o 
SOURCES = client_socket.c 

CFLAGS = -Wall -O -Wuninitialized -g -lpthread


.SILENT:

$(TARGET): $(OBJS)
	gcc -o $(TARGET) $(OBJS) -lm $(CFLAGS)

$(OBJS) : $(SOURCES) 
	gcc -c $(SOURCES)
	mv *.o ./obj

clean:
	-rm -rf ./obj/*.o
	-rm -rf servidor
